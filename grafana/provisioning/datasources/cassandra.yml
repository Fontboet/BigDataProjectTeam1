apiVersion: 1

datasources:
  - name: Cassandra
    type: hadesarchitect-cassandra-datasource
    access: proxy
    url: cassandra:9042
    json_data:
      keyspace: flights_db
      consistency: ONE
    isDefault: true
    editable: true